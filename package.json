{
    "scripts": {
        "build": "vite build",
        "test": "tsc --project tests/tsconfig.json && jest"
    },
    "devDependencies": {
        "@types/jest": "^30.0.0",
        "jest": "^30.3.0",
        "vite": "^8.0.10"
    },
    "dependencies": {
        "typescript": "^6.0.3"
    }
}
